web.tex needs pointing to my github project

Local good-prime Hecke decomposition, the tame-level topology bridge, and the first
`T`-side disjoint-support commutativity step are in place on `tv-good-prime-decomposition`;
the build is green again.
The global `T`-side bijection in `Concrete.lean` is currently parked as a `sorry` because the
`some/some` branch runs into a `Units.ext` coercion/elaboration blocker. The final
`HeckeAlgebra.instCommRing` commutativity proof is still downstream of that.
Some of the proof shape was adapted from the merged `polyproof/FLT` PR #38, with
attribution kept in the source comments.
